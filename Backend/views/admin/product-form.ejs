<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= producto ? 'Editar' : 'Nuevo' %> Producto</title>
  <link rel="stylesheet" href="/styles/admin.css" />
</head>
<body>
  <header>
    <h1><%= producto ? 'Editar' : 'Nuevo' %> Producto</h1>
    <a href="/admin/dashboard">Volver al Dashboard</a>
  </header>
  <main>
    <form action="<%= producto ? '/admin/productos/' + producto.id : '/admin/productos' %>" method="POST">
      <% if (producto) { %>
        <input type="hidden" name="_method" value="PUT" />
      <% } %>
      <div>
        <label for="name">Name:</label>
        <input type="text" id="name" name="name" value="<%= producto ? producto.name : '' %>" required />
      </div>
      <div>
        <label for="type">Type:</label>
        <input type="text" id="type" name="type" value="<%= producto ? producto.type : '' %>" required />
      </div>
      <div>
        <label for="price">Price:</label>
        <input type="text" id="price" name="price" value="<%= producto ? producto.price : '' %>" required />
      </div>
      <div>
        <label for="imageUrl">Image URL:</label>
        <input type="text" id="imageUrl" name="imageUrl" value="<%= producto ? producto.imageUrl : '' %>" />
      </div>
      <button type="submit"><%= producto ? 'Guardar Cambios' : 'Crear Producto' %></button>
    </form>
  </main>
</body>
</html>
